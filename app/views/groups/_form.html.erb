<%= simple_form_for(@group) do |f| %>

	<fieldset>
	  <div class="form-inputs">
	    <%= f.input :students %>
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit %>
	  </div>
	</fieldset>
<% end %>
